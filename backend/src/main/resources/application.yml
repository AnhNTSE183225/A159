spring:
  application:
    name: backend
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/A159
    username: theanh
    password: theanh
  jpa:
    hibernate:
      ddl-auto: update
